<nav class="navbar navbar-expand-lg navbar-light">
  <a class="navbar-brand" href="https://www.dol.gov"><%= image_tag  "logo.png" %></a>
  <a class="navbar-brand text-light" href="/">DEPARTMENT OF LABOR - NEWS RELEASE LOG</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">

    <% if user_signed_in? %>
    <div class="dropdown">
      <button class="btn btn-custom dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <%= current_user.first_name %>
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
        <!-- Uncomment the below link when the devise registerable module is enabled in the user model -->
        <!--<%= link_to 'Change Password', edit_user_password_path, method: :get, class: "dropdown-item" %>-->
        <% if current_user.admin? %>
          <%= link_to 'Manage Users', users_path, method: :get, class: "dropdown-item" %>
        <% end %>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>
    </div>
    <% end %>
   </ul>
  </div>
</nav>

